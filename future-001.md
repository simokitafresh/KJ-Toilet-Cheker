# Future Improvements (future-001.md)

将来的な改善・機能追加のリストです。

---

## 🖼️ F001: ダッシュボードに画像サムネイル表示

**優先度:** 中  
**ステータス:** ✅ 実装済み  
**実装方針:** CSS縮小方式（フロントエンドのみ）

### 概要
ダッシュボードのタイムラインに、各チェックの画像サムネイルを表示する。

---

### As-Is（現状）

```
┌────────────────────────────────────────┐
│  📊 本日のチェック履歴                   │
├────────────────────────────────────────┤
│  時刻     担当                          │
├────────────────────────────────────────┤
│  15:20    🐱                            │
│  14:10    🐶                            │
│  13:30    🐱                            │
│  ︙                                      │
└────────────────────────────────────────┘

※ 画像が見えない → 何を撮影したか分からない
```

---

### To-Be（改善後）

```
┌──────────────────────────────────────────────────────┐
│  📊 本日のチェック履歴                                 │
├──────────────────────────────────────────────────────┤
│  時刻     担当    画像                                │
├──────────────────────────────────────────────────────┤
│  15:20    🐱     [📷][📷]  ← タップで拡大             │
│  14:10    🐶     [📷][📷][📷]                         │
│  13:30    🐱     [📷][📷]                             │
│  ︙                                                    │
└──────────────────────────────────────────────────────┘

※ サムネイルで撮影内容を確認可能
※ タップでモーダル拡大表示
```

---

### 実装詳細

#### 1. バックエンド（API変更）
`SimpleTimelineItem` に `thumbnails` フィールドを追加：

```python
# schemas.py
class SimpleTimelineItem(BaseModel):
    time: str
    staff_icon: str
    thumbnails: List[str]  # 画像URLのリスト（NEW）
```

#### 2. フロントエンド（UI変更）
```tsx
// タイムラインの各行に追加
<td className="px-2 py-3 flex gap-1">
    {item.thumbnails.map((url, i) => (
        <img 
            key={i}
            src={url}
            className="w-12 h-12 object-cover rounded cursor-pointer"
            onClick={() => openModal(url)}
        />
    ))}
</td>
```

#### 3. モーダル表示
- 画像タップでフルサイズ表示
- スワイプで複数画像を切り替え（オプション）

### 方式選定理由

| 比較項目 | CSS縮小（採用） | バックエンド生成 |
|---------|----------------|-----------------|
| 実装コスト | ○ 低 | △ 高 |
| 初回ロード | △ やや遅い | ○ 高速 |
| ストレージ | ○ 増加なし | △ 2倍 |

**結論:** 元画像は既に1280px/75%圧縮済み（100-200KB）のため、CSS縮小で十分実用的。

---

## ⏰ F002: 主要チェックポイント未実施の記録

**優先度:** 高  
**ステータス:** ✅ 実装済み  
**実装方針:** バックエンド対応

### 概要
朝チェック・午後チェックなどの主要チェックポイント（MAJOR）が時間を過ぎても未実施の場合、タイムラインに「未実施」として記録・表示する。

---

### As-Is（現状）

```
┌─────────────────────────────────────────────────────┐
│  📋 主要チェックポイント                              │
├─────────────────────────────────────────────────────┤
│  開院前      ✅ 08:45                                │
│  午前終了時   🔴 未実施                               │
│  午後開始前   ✅ 13:30                                │
│  閉院時      ⏳ まだ                                  │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  📊 タイムライン                                      │
├─────────────────────────────────────────────────────┤
│  15:20  🟢 通常チェック  🐱                           │
│  14:10  🟢 通常チェック  🐶                           │
│  13:30  🟢 通常チェック  🐱  ← 午後開始前             │
│  ︙                                                   │
│  09:50  🟢 通常チェック  🐶                           │
│  08:45  🟢 通常チェック  🐱  ← 開院前                 │
│                                                       │
│  ❌ 午前終了時（12:00～12:30）の未実施が              │
│     タイムラインに表示されない                        │
└─────────────────────────────────────────────────────┘
```

**問題点:**
- 「午前終了時」が未実施でも、タイムラインには何も表示されない
- 後から履歴を見たときに、どこで未実施があったか分からない

---

### To-Be（改善後）

```
┌─────────────────────────────────────────────────────┐
│  📋 主要チェックポイント                              │
├─────────────────────────────────────────────────────┤
│  開院前      ✅ 08:45                                │
│  午前終了時   🔴 未実施                               │
│  午後開始前   ✅ 13:30                                │
│  閉院時      ⏳ まだ                                  │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  📊 タイムライン                                      │
├─────────────────────────────────────────────────────┤
│  15:20  🟢 通常チェック      🐱                       │
│  14:10  🟢 通常チェック      🐶                       │
│  13:30  🟢 通常チェック      🐱  ← 午後開始前         │
│  ︙                                                   │
│  12:30  🔴 午前終了時チェック未  ⚠️  ← NEW!          │
│  ︙                                                   │
│  09:50  🟢 通常チェック      🐶                       │
│  08:45  🟢 通常チェック      🐱  ← 開院前             │
└─────────────────────────────────────────────────────┘
```

**改善点:**
- 時間帯終了時に未実施だった場合、タイムラインに自動挿入
- 履歴として残るため、週次・月次レポートにも反映可能
- 視覚的に「いつ漏れがあったか」が一目瞭然

---

### 実装詳細（バックエンド対応）

#### 1. 定期ジョブの追加
```python
# 5分ごとに実行
def check_missed_major_checkpoints():
    now = get_jst_now()
    
    for checkpoint in get_major_checkpoints():
        if checkpoint.end_time < now.time():
            # 時間帯が終了している
            if not has_check_in_window(checkpoint):
                # 未実施 → MISSED_MAJORレコードを挿入
                create_missed_major_record(checkpoint)
```

#### 2. DBスキーマ
- `status_type` に `'MISSED_MAJOR'` を追加
- `staff_id` は NULL許容（またはシステムスタッフID）
- `major_checkpoint_id` を紐付け（どのチェックポイントが未実施か）

#### 3. フロントエンド表示
- タイムラインで `MISSED_MAJOR` は赤背景 + ⚠️アイコンで強調表示
- サムネイルなし（画像がないため）

---

## 📝 メモ

（追加の気になる点があればここに記載）

