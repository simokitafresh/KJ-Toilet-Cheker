# 現状の問題点まとめ (problem-001.md)

現在発生している不具合および改善が必要な点を以下にまとめます。

## 1. タイムゾーンの不一致によるデータの「ずれ」
システム全体で日本時間（JST）を基準とする必要がありますが、データベース（SQLite）とのやり取りにおいてUTCとJSTの変換に不備があります。

- **現象**: 記録された時間が9時間ずれて表示されたり、特定の時間帯のデータが無視されたりする。
- **原因**: 
    - データベースにUTCで保存されているが、検索時に `func.date(checked_at) == target_date` を使用しており、SQLiteがUTC基準で日付を判定している。
    - 日本時間の午前0時〜午前9時の間に作成されたレコードは、UTCでは「前日の日付」として保存されるため、当日のダッシュボードに表示されない。

## 2. 朝チェック（8:00〜8:50）が正常に動作しない
- **現象**: 朝チェックを実施しても「未実施」扱いになったり、ステータスが正しく更新されない。
- **原因**: 
    - 上記のタイムゾーン問題により、午前8時台のチェックがUTC基準では「前日のデータ」として扱われ、当日の集計から漏れている。

## 3. 休診日設定が反映されない
- **現象**: 設定画面で水曜や日曜を休診日に設定しても、ダッシュボード等で「休診日」表示にならず、アラートが停止しない。
- **原因**:
    - **インデックスの不一致**: 以前の設定（デフォルト等）では `0=Sunday` と案内されていた可能性があるが、現在のバックエンド実装（Pythonの `weekday()`）は `0=Monday` を基準としており、齟齬が発生している可能性がある。
    - **日付判定の誤り**: タイムゾーンのずれにより、サーバーが「現在」を前日または翌日と誤認して曜日の判定を行っている。

## 4. 解決の方向性
- **JST基準の範囲検索**: SQLレベルで `func.date` を使うのではなく、アプリケーション側で「当日のJST 00:00〜24:00」をUTCに変換した範囲（例: 前日15:00〜当日15:00 UTC）を指定してクエリを発行する。
- **曜日判定の統一**: 曜日の定義を Python/UI の標準である `0=Monday` に統一、または設定値の変換ロジックを修正する。
