# Future Improvements (future-003.md)

休診日・営業時間関連の機能追加リストです。

---

## 📅 F003: 休診日スキップ機能

**優先度:** 高  
**ステータス:** 未着手

### 概要
休診日はチェック不要のため、アラートをスキップする。

### 要件
- 管理画面で休診曜日を選択可能（例: 日曜・祝日）
- 休診日はダッシュボードに「休診日」と表示
- アラートは発生しない
- MISSED_MAJORレコードも挿入しない

### 実装案

#### 1. DBスキーマ
```python
# clinic_config テーブルに追加
# key: "closed_days", value: "0,6"  # 0=日曜, 6=土曜
```

#### 2. 管理画面（Admin）
- 曜日チェックボックス（月〜日）
- 選択した曜日を保存

#### 3. ダッシュボード
```
┌─────────────────────────────────────┐
│  📅 12月15日（日）                    │
│                                       │
│    🏥 本日は休診日です                │
│                                       │
└─────────────────────────────────────┘
```

---

## ⏰ F004: 診療時間設定

**優先度:** 高  
**ステータス:** 未着手

### 概要
診療時間外はアラートを停止し、定時にリセットする。

### 要件
- 診療終了時刻（例: 22:00 JST）を設定
- 診療開始時刻（例: 07:00 JST）を設定
- 診療時間外はアラート非表示
- 時間帯に応じた感情に配慮したメッセージ

### 実装案

#### 1. 設定（config.py / .env）
```python
BUSINESS_HOURS_START: str = "07:00"
BUSINESS_HOURS_END: str = "22:00"
```

#### 2. ダッシュボード表示ロジック
```python
def is_within_business_hours(now_jst: datetime) -> bool:
    start = parse_time(settings.BUSINESS_HOURS_START)
    end = parse_time(settings.BUSINESS_HOURS_END)
    return start <= now_jst.time() <= end
```

#### 3. 時間帯別メッセージ

**朝（診療開始前）**
```
┌─────────────────────────────────────┐
│  📅 12月16日（月） 06:30             │
│                                       │
│    ☀️ おはようございます！            │
│    今日もよろしくお願いします！        │
│                                       │
│    診療開始: 07:00                    │
└─────────────────────────────────────┘
```

**夜（診療終了後）**
```
┌─────────────────────────────────────┐
│  📅 12月15日（日） 22:30             │
│                                       │
│    🌙 お疲れさまでした！              │
│    ゆっくりお休みください             │
│                                       │
│    診療時間: 07:00 〜 22:00           │
└─────────────────────────────────────┘
```

#### 4. アラート停止タイミング
- 毎日22:00（診療終了）に自動停止
- 翌朝07:00（診療開始）に再開

---

## 📝 メモ

- F003とF004は組み合わせて使用可能
- 休診日かつ営業時間外 → 休診日表示を優先
